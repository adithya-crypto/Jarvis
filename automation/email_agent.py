"""
Email Agent - Placeholder for email integration.

To fully implement, you would need to configure:
- Gmail API with OAuth2 credentials (credentials.json)
- Or IMAP/SMTP with app passwords

For now, this provides stub functions that can be expanded later.
"""


def check_email():
    """Check for new emails. Requires Gmail API setup."""
    return ("Email integration is not yet configured. "
            "To enable it, set up Gmail API credentials in config/credentials.json.")


def send_email(to, subject, body):
    """Send an email. Requires Gmail API setup."""
    return ("Email sending is not yet configured. "
            "To enable it, set up Gmail API credentials in config/credentials.json.")


def summarize_inbox():
    """Summarize recent emails. Requires Gmail API setup."""
    return ("Email integration is not yet configured. "
            "To enable it, set up Gmail API credentials in config/credentials.json.")
